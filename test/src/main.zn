package test

String process() {
	return "new"
}

String process(String input) {
	return input
}

Void main() {
	String res = test$process("called process(String)")
	puts(res)
	puts(process())
	puts(test$process(process()))
}
